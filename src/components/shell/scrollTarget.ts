/**
 * DOM id of the shell's single scroll container (`<main>` in `App.tsx`).
 * Shared with `AppBar` so its `scroll-target` attribute can point at it.
 */
export const SCROLL_TARGET_ID = 'app-main';
